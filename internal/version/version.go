// Package version holds the ws-mixer-go release version: the one place it is
// written down, shared by wsmixer's default hello.agent.sdk_version and the
// conformance adapter's "ready" event.
package version

// SDK is this module's release version.
const SDK = "0.7.0"
